accept_eula = True # Accepts Minecraft EULA
custom_names = True # Allows you to set a custom name for the server folder, rather than the server type and the version
make_start_bat_script = True # Makes a start.bat script (use if on windows)
make_start_sh_script = True # Makes a start.sh script (use if on linux)
max_ram = "4096M" # Maximum allocated memory in megabytes, change M to G for gigabytes
min_ram = "1024M" # Minimum allocated memory in megabytes, change M to G for gigabytes
clear_terminal = True # Clears the terminal before running the script

